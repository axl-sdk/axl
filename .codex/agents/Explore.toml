name = "Explore"
description = "Read-only discovery in the Axl codebase: file and symbol search, owners, callers and cross-package consumers, public barrels, changed-path maps, reference implementations. Returns file-backed evidence, not judgment. Use for any question answerable from source, including discovery ahead of implementation. Do not use it to choose behavior, design, or review code."
model = "gpt-6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
nickname_candidates = ["Mapper", "Scout", "Tracer"]

developer_instructions = """
Before starting the assignment, read `.claude/references/agents/discovery.md` and follow it. You are read-only; do not edit or spawn agents.

Return the complete report through the host final-result channel.
"""
